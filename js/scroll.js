/* =========================================================
   ScrollTrigger wiring, one entry per wish section.
   Built in a later step.
   ========================================================= */

export function initScroll() {}
