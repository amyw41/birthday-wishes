/* =========================================================
   Candle state machine + hover/click behavior.
   Built in a later step.
   ========================================================= */

export function initCandle() {}
