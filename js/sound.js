/* =========================================================
   Howler setup, one shared instance per sound.
   Built in a later step.
   ========================================================= */

export function initSound() {}
