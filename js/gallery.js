/* =========================================================
   Gallery button + layout.
   Built in a later step.
   ========================================================= */

export function initGallery() {}
