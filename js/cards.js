/* =========================================================
   Card open/close logic.
   Built in a later step.
   ========================================================= */

export function initCards() {}
